const a = 'KoreaTimeGood'

sol = (a) => {
    let answer = 0;
    for(let i of a){
        if(i === i.toUpperCase()){
            answer ++;
        }
    }
    console.log(answer);
}
sol(a);

let str = 'KoreaTimeGood';


